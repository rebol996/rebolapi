# AI 协作规则

## 上下文控制

为了避免上下文窗口超限，处理本项目时请遵守：

1. 不要一次性扫描或读取整个项目。
2. 每次最多读取 2-3 个相关文件。
3. 读取文件前先说明为什么需要读取。
4. 如果需要了解项目结构，先列出目录摘要，不要展开所有文件内容。
5. 不要重复粘贴大段已读过的代码。
6. 每次只解决一个明确问题。
7. 修改代码前先说明修改计划。
8. 修改完成后给出简短验证步骤。
9. 如果任务变长，请主动生成“交接摘要”，方便开启新任务继续。

## 任务生命周期与上下文保护

1. 每个修复任务拆分为小阶段，每个阶段只修复 1-3 个相关文件的错误，完成后再进入下一个阶段。
2. 每完成一个阶段，生成交接摘要（已完成什么、还剩什么、下一步从哪个文件开始），方便新会话无缝接续。
3. 禁止在一个会话中连续修复超过 5 个文件的类型错误，超过时主动提醒用户开启新会话。
4. 使用 `git diff --stat` 或 `npm run typecheck 2>&1 | head -50` 获取错误概览，不要让工具返回完整错误列表。
5. 当对话轮次超过 30 轮或明显感觉上下文变长时，主动建议用户开启新会话并附上交接摘要。

## 默认工作方式

1. 先确认目标。
2. 再定位相关文件。
3. 然后做最小修改。
4. 最后运行验证命令。
5. 如果遇到报错，优先分析最可能原因，不要同时给太多方向。

## 输出要求

1. 回答使用中文。
2. 步骤要清晰。
3. 命令要完整。
4. 文件路径要明确。
5. 不要输出无关长篇解释。